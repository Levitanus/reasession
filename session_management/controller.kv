<Slave>:
    orientation: 'vertical'
    slave_address: slave_address
    conn_status: conn_status
    projects: projects
    TextInput:
        id: slave_address
        text: 'slave address'
        multiline: False
        on_text: root.valiadate()
    Label:
        id: conn_status
        text: 'not connected'
    BoxLayout:
        id: projects
        orientation: 'vertical'
<Slaves>:
    slaves_wid: slaves_widget
    # slave: Slave

    BoxLayout:
        # cols: 3
        padding: 20

        Label:
            text: 'slave servers:'
            size_hint_x: None
            width: 100
        BoxLayout:
            id: slaves_widget
            orientation: 'horizontal'
            # width: 300
            Slave
        Button:
            text: 'add slave'
            size_hint_x: None
            width: 100
            on_press: root.action()